<div class="container py-4">
  <div class="row">
    <div class="col-md-8 col-lg-6 mx-auto">
      <div class="mb-4">
        <h2 class="fw-bold mb-0">Edit Destination</h2>
        <p class="text-muted small">Update settings for <%= @destination.name %></p>
      </div>

      <div class="card">
        <div class="card-body">
          <%= render 'form', collection: @collection, destination: @destination %>
        </div>
      </div>
    </div>
  </div>
</div>