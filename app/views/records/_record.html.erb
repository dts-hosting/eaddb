<%= tag.div id: dom_id(record), class: "card mb-3" do %>
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-md-3 text-start">
        <div class="d-flex align-items-center mb-1">
          <span class="text-muted small me-2">Record</span>
          <span class="badge <%= record_status_badge_class(record.status) %> text-light">
            <%= record.status %>
          </span>
        </div>
        <span class="fw-medium"><%= record.ead_identifier || "No EAD ID" %></span>
      </div>

      <div class="col-md-3 text-start">
        <span class="text-muted small d-block mb-1">Source</span>
        <span class="fw-medium"><%= link_to record.collection.source.name, source_path(record.collection.source) %></span>
      </div>

      <div class="col-md-3 text-start">
        <span class="text-muted small d-block mb-1">Collection</span>
        <span class="fw-medium"><%= link_to record.collection.name, collection_path(record.collection) %></span>
      </div>

      <div class="col-md-2 text-start">
        <span class="text-muted small d-block mb-1">Last Modified</span>
        <span class="fw-medium"><%= record.modification_date.strftime("%B %d, %Y at %H:%M") %></span>
      </div>

      <div class="col-md-1 text-end d-flex justify-content-end align-items-center">
        <div class="btn-group" role="group" aria-label="Record actions">
          <%= link_to record_path(record), class: "btn btn-outline-primary btn-sm", title: "View collection details" do %>
            <i class="bi bi-eye"></i>
          <% end %>
          <%= link_to record_path(record), class: "btn btn-outline-danger btn-sm", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this collection?" }, title: "Delete record" do %>
            <i class="bi bi-trash"></i>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>